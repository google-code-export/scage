package crosseszeros

import su.msk.dunno.scage.support.ScageLibrary._
import su.msk.dunno.scage.handlers.net.NetServer
import su.msk.dunno.scage.handlers.AI
import java.lang.String

object CZServer {
  /*case class MyString(val s:String)
  properties = "cz-properties.txt"

  NetServer.greetings = "CrossesZeros, v 0.1"

  val board = Array.ofDim[Int](5,5)

  var is_crosses_move= true
  val logEntry = "(\d+) (\d+)".r
  AI.registerAI(() => {
    if(NetServer.clients.length == 2) {
      if(is_crosses_move) {
        val cross = NetServer.clients(0)
        if(cross.hasNewIncomingData) {
          val data = cross.incomingData
          if(data.has("raw")) data.getString("raw") {

          }
        }
      }
    }
  })*/
}