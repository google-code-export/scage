package mystate

/*case class StringState(key:String, stringval:String)
case class IntState(key:String, intval:Int)
case class FloatState(key:String, floatval:String)
case class BoolState(key:String, boolval:Boolean)

class StateList extends List[Product] {

}*/

object NewStateTest extends Application {
  /*val my_state = IntState("damage", 56)
  val my_state2 = StringState("t2", "gygy")
  
  val state_list = new StateList(my_state, my_state2)  
  
  state_list.foreach(_ match {
    case IntState("damage", damage) =>
      println("received " + damage + " damage")  
  })*/
}
